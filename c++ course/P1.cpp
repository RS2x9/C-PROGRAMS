#include<iostream>
// This line includes the input-output stream library (iostream), which allows you to use 
//  std::cout and std::cin for output and input.

int main()
{
    std::cout<<"Hello";  // std::cout is used to print output to the console. 
                         //It belongs to the std (standard) namespace. The << operator is used to send the string "Hello" to the console.
    return 0;
}