// strlen(str)
#include<stdio.h>
#include<string.h>
int main()
{
    char name[]="sagar";
    int length=strlen(name);  // typecasting strlen(string)
    printf("Length of name : %d",length);
    return 0;
}